machine:
  node:
    version: 4.2.6

general:
  branches:
    only:
      - master
      - beta

dependencies:
  override:
    - npm install -g serverless
    - npm ls

test:
  override:
    - serverless deploy --stage $CIRCLE_BRANCH --region us-east-1