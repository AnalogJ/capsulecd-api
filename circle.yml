machine:
  node:
    version: 4.2.6

general:
  branches:
    only:
      - master

dependencies:
  override:
    - npm install -g serverless
    - npm install

test:
  override:
    - serverless function deploy -s dev -r us-east-1 -a
